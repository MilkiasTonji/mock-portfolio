import React from 'react'

const useThemeSwitcher = () => {
    // css feature to detected the user's color preference
    const preferDarkQuery = "(prefer-color-scheme: dark)";

    

  return (
    <div>useThemeSwitcher</div>
  )
}

export default useThemeSwitcher